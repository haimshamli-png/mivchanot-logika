{
  "name": "logic-tubes",
  "version": "1.0.0",
  "private": true,
  "description": "מבחנות הלוגיקה - static PWA packaged with Capacitor",
  "scripts": {
    "build": "node scripts/build-capacitor-www.js",
    "test": "node test/contracts.test.js && node test/daily-challenge.test.js && node test/valves.test.js && node test/portals.test.js && node test/world9-valves-bfs.test.js && node test/world10-portals-bfs.test.js && node test/pigment-mixing.test.js && node test/pigment-world-bfs.test.js && node test/world6-bfs.test.js && node test/level-quality.test.js && node test/difficulty-monotonicity.test.js && node --check contracts.js && node --check daily-challenge.js && node --check valves.js && node --check portals.js && node --check game.js && node --check levels.js && node --check pigment-mixing.js",
    "cap:sync": "npm run build && npx cap sync android",
    "cap:open:android": "npm run cap:sync && npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.0",
    "@capacitor/core": "^8.4.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.0"
  }
}
