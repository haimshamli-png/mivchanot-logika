{
  "name": "logic-tubes",
  "version": "1.0.0",
  "private": true,
  "description": "מבחנות הלוגיקה - static PWA packaged with Capacitor",
  "scripts": {
    "build": "node scripts/build-capacitor-www.js",
    "test": "node test/pigment-mixing.test.js && node test/pigment-world-bfs.test.js && node test/world6-bfs.test.js && node test/level-quality.test.js && node --check game.js && node --check levels.js && node --check pigment-mixing.js",
    "cap:sync": "npm run build && npx cap sync android",
    "cap:open:android": "npm run cap:sync && npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.0",
    "@capacitor/core": "^8.4.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.0"
  }
}
