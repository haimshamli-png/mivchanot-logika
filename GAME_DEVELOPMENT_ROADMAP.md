# מפת פיתוח למשחק מבחנות הלוגיקה

עודכן: 2026-09-13

## עיקרון מנחה

הקושי של שלב נמדד לפי **סוג החשיבה** — מהלכים רגרסיביים, ייחודיות הפתרון, הסתעפות, עומק קבירה ומחסור בחיץ (`scripts/difficulty.js`, ציון משולב 0–100 ו-`cogLoad`). מספר המהלכים האופטימלי הוא רכיב משני (14% מהמשקל) ומשמש רק לזיהוי השלב ולחישוב כוכבים. תגיות הדרגה (מבוא / תרגול / פאזל / מעבדה / מומחה / מאסטר) נגזרות מהציון, לא מהאורך.

## מה הוטמע בספטמבר 2026

### מסלול ותוכן
- סדר עולמות חדש: 1 מהדורה ראשונה → 3 ג'וקר → 6 שיפט → 4 נעולות → 8 פיגמנטים → 10 צינורות → 11 מסלול מומחה. הסדר נקבע בשדה `order` של כל עולם.
- הוסרו 11 שלבים שחזרו על לוח של שלב אחר במסלול הגלוי; `test/level-quality.test.js` אוסר לוח חוזר (אותו לוח פתיחה ואותו יעד).
- עולם 4: 14 שלבים, החצי השני נבנה סביב **מנעול-תנאי** (`locks: [{ tubeIndex, until: { tube, equals } }]`) — מבחנה שנפתחת כשמבחנת המפתח 🗝 מגיעה למצב שעל התג. שיא 90.
- עולם 6: שני בוסים חדשים עם מקורות מעורבים (מיון + המרה יחד). שיא 81.
- עולם 10 נבנה מחדש סביב **צינור תחתי** (`portals: [{ pair: [chute, exit], mode: 'bottom' }]`): כדור שנכנס לפתח מחליק אל תחתית המבחנה התאומה בלי חוקי צבע. מבוא במהלך אחד, ערימות שכבות, צינור לשיפט, מנעולי-תנאי, ובסוף "מדף מוצרים" שבו סגול, ירוקים ושחורים חייבים להיווצר בסדר הפוך. שיא 97 — השלב הקשה במשחק.
- עולם 11 "מסלול מומחה": שישה שלבי מאסטר (שסתומים מתהפכים, מבחנות שבירות, המפלצת של עולם 4). נפתח לפי 20 חותמות אתגר, לא לפי כוכבים.
- עולם 7 (מערבל → ג'וקר) הוסר מהמנוע ומהנתונים. עולמות 2, 5, 9 נשארים מוסתרים כחומר גלם.
- שערי כוכבים: 43–55% מהכוכבים שלפני העולם (בדיקה אוטומטית). בתוך עולם אפשר לדלג על שלב תקוע אחד.

### UX
- כותרת המשחק מראה `מהלכים / יעד`, מד כוכבים חי ותגית דרגה.
- יעד-רפאים: צלליות הקלף בתוך מבחנות המשחק; כדור שהגיע למקומו הסופי מקבל טבעת.
- צ'יפי כללים ויזואליים (מתכונים, מחזור שיפט עם כיוון, מנעול, צינור) במקום פסקת טקסט.
- אנימציית מעבר בקשת, צלילים מסונתזים (WebAudio) ורטט (navigator.vibrate / Capacitor Haptics), כולם ניתנים לכיבוי.
- גופנים עבריים: Suez One + Frank Ruhl Libre (נוסטלגי), Heebo (מודרני).
- במסך צר: מ-6 מבחנות ומעלה שתי שורות, כדורים 44px.
- מסך הבית: כרטיס "המשך", אריחי יומי ו-Time Attack, מדף עולמות עם פס התקדמות, איפוס עבר להגדרות עם אישור כפול.
- מסך ניצחון: כפתור ראשי אחד, השוואה לשיא, "נסה שוב ל-3★", שיתוף לניסוי היומי.
- רמז: שלושת המהלכים הראשונים של פתרון אופטימלי (מ-`level-meta.js`), מוותר על חותמת "אופטימום".
- סימני נגישות על כדורים (הגדרה).
- לחיצה על מבחנה נעולה או על צינור שמחזיר אינה נספרת כהפרה של "יד נקייה".

### מצבים
- Time Attack: לוחות נוצרים בזמן אמת (`ta-generator.js`) עם אימות BFS, סבבים מסלימים, בלי אנימציית פתיחה.
- ניסוי יומי: המאגר מסונן לעולמות שנפתחו, רצף ימים, שיתוף בסגנון Wordle.

### תשתית
- `npm run annotate` מפיק את `level-meta.js` (דרגה, ציון, רמז); `test/level-meta.test.js` מוודא שהוא מסונכרן עם `levels.js`.
- `test/difficulty-monotonicity.test.js` בודק גם הסלמה בין עולמות: שיא כל עולם ≥ שיא העולם שלפניו.
- `npm run difficulty` מדפיס את הטבלה המלאה; `-- --check` למונוטוניות; `-- --all` כולל עולמות מוסתרים.

## שלב ד' (13.9.2026, המשך)

- עולם 12 "המעבדה המצונרת": **צנרת** — `pipes: [[a, b]]` (צינורות דו-כיווניים) ו-`oneWay: [[from, to]]`; שופכים רק דרך צינור, המבחנות המחוברות מוארות כשבוחרים מקור, וצינורות מצוירים על הלוח. 10 שלבים, קו → מזלג → אבני דריכה → טבעת חד-כיוונית → שיפט בצומת → מעבדה בצומת → גשר עם מנעול-תנאי → בוס עם צינור תחתי. שיא 84 (בקנה המידה החדש).
- עולם 13 "הצנטריפוגה": `centrifuges: [i]` — מבחנה שמתהפכת ברגע שהיא מתמלאת. "ממלאים כדי לחפור", הג'וקר כמפתח היפוך שנקבר. 10 שלבים, שיא 100 — השלב הקשה במשחק.
- **כדור מתקשה**: מחרוזת כדור עם תקציב (`'R2'`); כל מהלך מוריד אחד, באפס הכדור מתקשה במקומו. המספר מצויר על הכדור. משמש במסלול המומחה.
- מסלול המומחה הורחב ל-12 שלבים (3 כדורים מתקשים + 3 שילובי מכניקות), ממוין לפי ציון 54 → 87.
- **ניסוי השבוע** (`serial.js`): עונה "ההזמנה השחורה" — 7 פרקים משורשרים (היעד של היום הוא הפתיחה של מחר, אלמנט חדש בכל יום: מערבל, מבחנה חתומה, ממיר, צבע חדש, צינור, שחור). פרק אחד ביום מרגע ההתחלה, "בפרקים הקודמים" מנגן את הפתרון האופטימלי של אתמול על הלוח האמיתי (הפתרונות ב-`level-meta.js`).
- **סולם הכיול**: עשרה שלבים מהעולמות שנפתחו, מסודרים לפי עומס קוגניטיבי ונבחרים לפי התאריך; ניסיון אחד ליום, בלי חזרה ובלי רמז, נפילה ברגע שהכוכב השלישי אבד; תוצאה ושיא נשמרים, שיתוף בסגנון Wordle.
- בדיקות חדשות: `mechanics.test.js` (צנרת, צנטריפוגה, כדור מתקשה בפותר), `serial.test.js` (רציפות העונה וכללי הפתיחה), `new-worlds-bfs.test.js`.

## שלב ה' (13.9.2026) — שלושת הפריטים האחרונים מספר ההחלטות

- **ניסוי יומי בשלוש דרגות**: המאגר המסונן (עולמות שנפתחו, 8–24 מהלכים) מדורג לפי הציון המשולב מ-`level-meta.js` ונחתך לשלישים — קל / בינוני / קשה (`DAILY_TIERS`, `splitPoolByTier`, `pickDailyChallenges` ב-`daily-challenge.js`). כלל מעבדה אחד ליום, חידה אחת לכל דרגה. לחיצה על האריח פותחת בוחר דרגות עם השלב, תגית הדרגה והתוצאה של היום; מסך הניצחון מציע "לדרגה הבאה". התוצאות נשמרות לפי יום ולפי דרגה (`{ modifierId, tiers: { easy, medium, hard } }`; רשומות ישנות נקראות כדרגה בינונית). הרצף נשמר אם נפתרה דרגה כלשהי. השיתוף מציין את הדרגה ושורת `🟢✓ 🟡· 🔴○`.
- **ניקוד Time Attack לפי דיוק**: כל לוח שווה עד 100 נקודות כפול (אופטימום ÷ מהלכים) (`boardScore`, `runScore` ב-`ta-generator.js`). הניקוד רץ בכותרת, ובמסך הסיום מופיעים לוחות, נקודות, אחוז דיוק ושני שיאים נפרדים (`tubes-ta-best`, `tubes-ta-best-score`).
- **פרס כל 25 חותמות אתגר**: כל 25 חותמות = תואר מעבדה (`STAMP_RANKS` ב-`contracts.js`, 12 תארים ל-300 חותמות) + **רמז חופשי** ששומר על חותמת "אופטימום" (`tubes-free-hints-used`; אסימונים = אבני דרך − שנוצלו). מסך הבית מראה את התואר, המרחק לפרס הבא ורמזים חופשיים; מסך הניצחון מציג באנר כשחוצים אבן דרך; כפתור הרמז מציג "רמז · N חופשי" עד שהריצה חויבה.
- בדיקות: הורחבו `daily-challenge.test.js` (שלישים, צרור יומי, נרמול רשומות ישנות, שיתוף), `ta-generator.test.js` (ניקוד) ו-`contracts.test.js` (אבני דרך, תארים).

## סבב בדיקות על הבוסים (13.9.2026)

נבדקו: עולם 10 שלבים 8–10, עולמות 12 ו-13 (8–10), 12 שלבי מסלול המומחה ופרק 7 של הסדרה — ניתוח מרחב מצבים מלא (מצבים ללא מוצא, מהלכי פתיחה קטלניים), סוכן ביקורת עצמאי וטעינה בדפדפן ברוחב טלפון.

- **ממצא הוגנות**: 20 מ-22 שלבים ניתנים להסקה מהמסך. שני כללים היו מיושמים בקוד אך לא כתובים בשום צ'יפ: (א) כדור שהתקשה במעבדה נבלע בערבוב (מסלול מומחה 8 ו-11 בנויים על זה, והצ'יפ אמר "לתמיד"); (ב) כדור מצינור תחתי ממלא צנטריפוגה ומפעיל היפוך (עולם 13 שלבים 9–10, מומחה 7). תוקן בטקסט הצ'יפים ובכותרות התגים (🌪, 🗝 כשמבחנת המפתח היא צנטריפוגה).
- **מלכודות פתיחה** (מהלך ראשון שהופך את השלב לבלתי פתיר, ניתן לביטול רק ב-Undo): עולם 13 שלב 8 (3 מ-5), מומחה 2 (3 מ-6), מומחה 8 (1), מומחה 10 (3 מ-4). הבוסים של עולמות 10, 12, 13 ופרק 7 נקיים ממלכודות בשלושת המהלכים הראשונים. נוסף טוסט חד-פעמי לשלב על מהלך בלתי הפיך (צינור, ערבוב, היפוך) וטוסט כשכדור מתקשה מחוץ לביתו / במעבדה.
- **פריסה בטלפון (375×812)**: לוחות של שתי שורות גלשו ב-130–360px — הצ'יפים והפוטר (חזור/רמז/אפס) היו מתחת לקצה המסך בכל הבוסים. תוקן: מצב `rows-2` קומפקטי (מבחנות 42px לכדור, כרטיס יעד מוקטן, צ'יפים בשורה אחת נגללת) ופוטר דביק. כל הבוסים נכנסים במסך אחד; שני שלבי המומחה עם 7–8 מבחנות (4, 12) עדיין נגללים מעט.
- הערות למעצב (לא תוקן): מומחה 12 (הסיום) הוא השלב הסלחני בעולם (מיליון מסלולים, 0 מלכודות) ו"מאסטר" שלו מגיע מהמכפיל; מומחה 5 יושב בדיפ קוגניטיבי (32); עולם 10 שלב 9 ועולם 13 שלב 10 מסלימים באורך/מכפיל בלבד (Δcog 0).

## iOS (13.9.2026)

- פלטפורמת iOS נוספה (`npx cap add ios`, Capacitor 8, SwiftPM — בלי CocoaPods) עם `@capacitor/haptics`. תיקיית `ios/` בגיט; בנייה: `npm run build && npx cap sync ios && npx cap open ios`. אייפון בפורטרט בלבד (Info.plist).
- נבדק בסימולטור iPhone 17 Pro (Xcode 26.4): המסך הראשי, שלב ומסך ניצחון נראים נכון עם safe-area; קריאות `Haptics.impact` מגיעות לתוסף ונענות (רואים ביומן הגשר `⚡️ To Native -> Haptics impact`); WebAudio מתחיל לנגן אחרי המגע הראשון ו-AudioSession מופעל. תחושת הרטט עצמה נבדקת רק במכשיר פיזי.
- אנדרואיד: התוסף Haptics מסונכרן גם לשם (`npx cap sync android`); עד עכשיו אנדרואיד השתמש ב-navigator.vibrate.

## הבא בתור

1. כרטיס תוצאה שבועי לסולם.
2. עונה שלישית לניסוי השבוע (הטיזר של עונה 2 מבטיח "ירוק מתוך צהוב").
3. iOS: אריזת Capacitor ל-iOS ובדיקת רטט/צלילים במכשיר.
